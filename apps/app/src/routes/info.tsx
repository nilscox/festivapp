import { Info } from 'lucide-react';

import { Placeholder } from '../components/placeholder.tsx';

export function InfoPage() {
  return <Placeholder title="Festival Info" icon={<Info size={24} strokeWidth={2} />} />;
}
