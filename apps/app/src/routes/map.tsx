import { Map as MapIcon } from 'lucide-react';

import { Placeholder } from '../components/placeholder.tsx';

export function MapPage() {
  return <Placeholder title="Site Map" icon={<MapIcon size={24} strokeWidth={2} />} />;
}
